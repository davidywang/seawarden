---
title: "Untitled"
author: "Zack Dinh"
date: "October 21, 2019"
output: html_document
---

```{r, echo=FALSE}
library(RAC)
library(dplyr)

userpath <- "/Users/Zack/0_seawarden/r/RAC_seabass/"
setwd(userpath) #working directory
```

```{r}
#create RAC folders for each site
Bass_pop_skeleton(userpath)
```

```{r}
#load environmental variables
forcings <- Bass_pop_dataloader(userpath) 
output <- Bass_pop_main(userpath, forcings)
```
